# Hugging Face Inference API token (never commit the real .env)
HF_TOKEN=

# Optional: force mock/test generation (Pillow placeholders)
# ECHO_MOCK_GENERATION=1

# Optional: override project root discovery
# ECHO_PROJECT_ROOT=

# Optional: path to a pre-downloaded local diffusion model (no auto-download)
# ECHO_LOCAL_MODEL_PATH=
