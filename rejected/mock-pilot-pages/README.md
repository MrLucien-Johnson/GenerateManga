# Mock pilot pages (non-production)

These PNGs were generated by the mock backend for pipeline testing only.
They are **not** production-eligible and must not be copied back into `approved/`
for KDP / PDF builds.

Moved from `approved/` during Phase 12 (real Kaito design gate reset).
